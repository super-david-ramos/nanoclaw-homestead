# Welcome

This is the test fixture vault. The Phase 2 fs-watcher demo scans this tree
to verify the production code path (container mount + vault-hash walker +
state-file diff) without touching the household's real Obsidian vault.

Add notes under `areas/`, `projects/`, `resources/`, `archive/`, or
`conversations/` to extend the fixture.
