# Follow-ups

Things to pick up in a future session — not blocking the current phase but worth not dropping. Add a row when you defer work; remove the row when it's done.

## Resume pointer (session ending 2026-05-03)

**Branch:** `feat/host-bun-migration` (20 commits ahead of `main`, NOT pushed). Bun migration done, router test coverage substantially closed, Homestead Nanobot bundle wired into `/setup`, README + CHANGELOG updated for the fork-scoped changes.

**To pick up:** `git checkout feat/host-bun-migration && bun test --isolate` should be 318/318 green and `bun run typecheck` clean. Live launchd service is running on bun via the bundle (verify with `launchctl list | grep nanoclaw` + `sfltool dumpbtm | grep -A 5 'Homestead Nanobot'`).

**Active next task** the user named when this session paused: rewriting `nanoclaw.sh` (and the chain it touches: `setup/install-node.sh`, `setup/probe.sh`'s NODE_VERSION detection, the `pnpm --silent run setup:auto` hand-off) to install Bun instead of Node + pnpm. Required for fresh-machine deployment on the user's Mac Mini (laptop is already set up, doesn't need it).

See [phase-bun-migration.md](phases/phase-bun-migration.md) "Reports" section for the full migration log + lessons learned + rollback recipe + the SIGTERM post-mortem (BTM bootout from System Settings → Login Items after the plist edit changed bun's signing identity).

## Open

| Entry | Origin | Notes |
|---|---|---|
| **`nanoclaw.sh` installer + `setup/install-node.sh` + `setup/probe.sh` still install Node + pnpm** — the public install entry point is broken post-bun-migration for fresh machines. The bash-side first chunk runs `setup/install-node.sh`, the probe checks `NODE_VERSION`, and the hand-off uses `pnpm --silent run setup:auto`. Needs: a `setup/install-bun.sh`, probe checks `BUN_VERSION`, hand-off uses `bun run setup/auto.ts` (since tsx is gone too). Doesn't affect existing installs (laptop is already set up); blocks the Mac Mini fresh deployment. | 2026-05-03 autopilot session, surfaced when user asked "is the README updated" | The hand-off path is the most fragile bit — `setup:auto` ran via `pnpm` previously; the bun equivalent needs to handle the bash → ts handoff cleanly. |
| **Translated READMEs (`README_zh.md`, `README_ja.md`) reference Node + pnpm in install requirements** — same four spots as the English README at the same line numbers. Not translated as part of the bun-migration session (don't trust auto-translation for public-facing prose). Either sync at translation time, or wait for upstream parity. | 2026-05-03 | Lowest urgency — translated READMEs are auxiliary, the English version is canonical. |
| **`writeOutboundDirect` had been opening `outbound.db` read-only** — surfaced + fixed during the autopilot router-coverage sweep (commit `9c3704d`), but the deeper question is "what other host-side code uses `openOutboundDb` for writes." The function naming is misleading — `recordDroppedMessage` writes to a table named `unregistered_senders`, and `openOutboundDb` is named like a generic accessor but is in fact read-only. A pass to consistency-rename or to add explicit `…ReadOnly`/`…Write` suffixes would prevent the next bug like this. | 2026-05-03 | Cosmetic-but-load-bearing. Defer until next time someone touches session-db.ts. |
| **delivery.ts and host-sweep.ts coverage is still low** — coverage report (regen via `bun test --isolate --coverage`) shows delivery.ts at ~49% line / 47% func, host-sweep.ts at ~21% line / 22% func. Both are operational hot paths (every 1s and 60s respectively in production). The router sweep gave a template; the same pattern applies. | 2026-05-03 | Realistic batch: 8-12 tests for delivery.ts (system-action handling, error paths, race-recovery), 6-10 for host-sweep.ts (sweep tick body, stale-detection, recurrence-fan-out). Each ~1-2 hours focused work. |
| **Lift `src/router.ts` branch coverage toward 100%** — substantially addressed in the autopilot session ending 2026-05-03: dedicated `src/router.test.ts` added with 23 tests covering channel-request gate flows (no-agent-wired, denied, gate-registered, gate-not-registered), engage_mode=mention/mention-sticky variants (DM short-circuit, group-with-prior-session, group-no-session), no_agent_engaged audit row + accumulate exception, command gate filter+deny, replyTo redirect, all four setter overwrite warnings, unknown engage_mode default, adapter thread-strip (non-threaded), per-thread session-mode override (threaded + group), DM exception (is_group=0). Found and fixed a latent bug in `writeOutboundDirect` (was opening outbound.db read-only via `openOutboundDb`; never fired in prod because admin-only household). Remaining gaps deferred — see commit `8d8a2ce` body. | session ending 2026-04-30 (engage_pattern silent-fail-open bug); largely closed 2026-05-03 | Done unless `coverage/index.html` (regenerable via `bun test --isolate --coverage`) reveals specific high-value branches still uncovered. |
| **Native Telegram voice-note UI for outbound TTS** — chat-sdk's Telegram adapter ships all files via `sendDocument`, so Phase 1 voice replies arrive as audio attachments with a play button rather than as the native voice-note UI (waveform, in-app inline player). To get the native UI: bypass chat-sdk for `audio/ogg` mime types and call Telegram's `sendVoice` API directly (or fork-patch `@chat-adapter/telegram`). Functional voice replies work today; this is a UX polish. | 2026-05-02 (Phase 1 close) | Most likely path: a small `voice-note-send` shim that detects `audio/*` files in the outbound, intercepts before `bridge.send`, hits Telegram `sendVoice` directly. Keep the chat-sdk path for everything else. |
| **Drop or repurpose `users.prefers_voice_replies`** — migration 014 shipped this column for the original "per-user opt-in" rule, then we superseded that rule with "match the medium of the triggering message" and the column went unused. Decide: drop in a new migration, or keep as a future "always voice no matter what" override that layers on top of the medium-match rule. No urgency; not load-bearing today. | 2026-05-02 (Phase 1 close) | Schema-wise either is fine; SQLite tolerates unused columns indefinitely. Decision is product-shaped, not technical. |
| **Per-file-hash fs-watcher state** — current `fsWatcherDecide` records aggregate hash + fileCount only. The decision passed to the agent says "something changed" but not "what changed." Upgrade path: persist `Map<relPath, sha256>` in the state file, compute `{added, removed, modified}` diff in `fsWatcherDecide`, surface as `data.changed`. Trade-off: state file grows linearly with vault size (~32 bytes/file). For a 5-figure-file vault that's hundreds of KB — fine on disk but slows JSON parse on every tick. | 2026-05-02 (Phase 2 close) | Defer until the agent's responses to fs-watcher wakes feel un-targeted ("I see the vault changed but don't know what to look for"). Until then, the agent can re-scan or use the bash sandbox if it needs detail. |
| **Pre-existing bun-test failures in `factory.test.ts` + `poll-loop`** — `bun test` against the agent-runner tree (1 fail / 2 errors as of Phase 2 close, present on `main` before Phase 2 work) — `poll-loop.ts:273` calls `getInboundDb()` which opens `DEFAULT_INBOUND_PATH` read-only at import time when test env doesn't have it; `factory.test.ts` then trips on describe-after-completion. Not a Phase 2 regression. Likely needs the test env to either set `INBOUND_DB` to a temp file or to lazy-open. | 2026-05-02 (Phase 2 close) | Run `cd container/agent-runner && bun test` to repro. Probably a 1-2-line fix (lazy `_inbound` initialization or test-env override) but bun-side, so flag for a session that wants to dig into the container test infra. |
| **Live-container smoke step in `run.sh`** — Phase 2 demo's section 3 currently observes `vaultMissing:true` because the running family container predates the `container.json` mount edit. Cleanest path forward: an optional `--respawn` flag that `docker rm -f`s the family container before testing. Currently invasive enough I left it manual. | 2026-05-02 (Phase 2 close) | Adding the flag is mechanical; the design call is "should the demo ever kill a live container?" Probably not by default — but a documented `--respawn` opt-in is fine. |
